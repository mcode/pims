FROM node:14-alpine
WORKDIR /home/node/app/pims
COPY --chown=node:node . .
# RUN npm install; The dockerRunnerDev.sh runs npm install
# EXPOSE 3000; This container does not listen for any incoming traffic. Thus EXPOSE is not needed. 
CMD ./dockerRunnerDev.sh